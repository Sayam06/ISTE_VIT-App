<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sayam.iste">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ISTE">
        <activity android:name=".ViewMeeting"></activity>
        <activity android:name=".ViewAnnouncements" />
        <activity android:name=".coreMemberDetails" />
        <activity android:name=".BoardDetails" />
        <activity android:name=".BoardMeeting" />
        <activity android:name=".BoardAnnouncements" />
        <activity android:name=".boardProfile" />
        <activity android:name=".coreProfile" />
        <activity android:name=".memberLogin" />
        <activity android:name=".boardLogin" />
        <activity
            android:name=".Loginboard"
            android:label="@string/title_activity_loginboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>